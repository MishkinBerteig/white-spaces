name: Node.js CI

on: [push]

jobs:
  serve:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js 
      uses: actions/setup-node@v1
    - run: npm install
    - run: npm run start
    # - run: npm test
      env:
        CI: true
